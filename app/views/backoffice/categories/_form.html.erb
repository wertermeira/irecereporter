<%= simple_form_for([:backoffice, @category]) do |f| %>
  <%= f.input :name %>
  <%= f.input :show_on_home, as: :boolean, input_html: { data: { toggle: 'toggle', size: 'sm'} } %>
  <%= f.button :submit, class: 'btn-success' %>
<% end %>
