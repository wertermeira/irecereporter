<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="wertermeira@gmail.com">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'backoffice', media: 'all' %>
  </head>

  <body>
    <header class='page-header'>
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <a class="navbar-brand" href="#">Home</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item <%= menu_current_link if controller_name == 'posts' %>">
              <a class="nav-link" href="<%= backoffice_posts_url %>">Posts</a>
            </li>
            <li class="nav-item <%= menu_current_link if controller_name == 'banners' %>">
              <a class="nav-link" href="#">Banners</a>
            </li>
            <li class="nav-item <%= menu_current_link if controller_name == 'categories' %>">
              <a class="nav-link" href="<%= backoffice_categories_url %>">Categorias</a>
            </li>
            <li class="nav-item <%= menu_current_link if controller_name == 'pages' %>">
              <a class="nav-link" href="<%= backoffice_pages_url %>">Páginas</a>
            </li>
          </ul>
        </div>
      </nav>
    </header>
    <div role='main' id="page">
      <div class='container'>
        <%= yield %>
      </div>
    </div>

    <%= javascript_pack_tag 'backoffice' %>
  </body>
</html>
